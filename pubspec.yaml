name: social_downloader
description: Professional Social Downloader App with Admin Panel
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  gal: ^2.2.1
  path_provider: ^2.1.2
  share_plus: ^7.2.1
  url_launcher: ^6.2.4
  video_player: ^2.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
